apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: soat-orders
  labels:
    app: order-service
spec:
  type: LoadBalancer
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080